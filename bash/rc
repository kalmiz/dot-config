
export FZF_DEFAULT_COMMAND="rg --files"
export NVIM_LISTEN_ADDRESS="/tmp/nvim.sock"
export BROWSER="open -a safai"
export EDITOR=vim

alias ll="ls -l"
alias vi=$EDITOR
alias e="$EDITOR \$(fzf)"
alias :q="exit"
alias jjbini="docker exec jenkins cat /var/jenkins_home/jenkins-jobs.ini | sed 's/localhost/192.168.99.100/'"

if [ -z "$SCMPUFF_GIT_CMD" ]; then
	alias gs="git status"
fi

if [ $(uname) == "Darwin" ]; then
	[[ -s $(brew --prefix)/etc/profile.d/autojump.sh ]] && . $(brew --prefix)/etc/profile.d/autojump.sh
	export PS1='\W\$ '
fi

if [ -d $HOME/Projects ]; then
	export GOPATH=$HOME/Projects/go
	export PATH=$GOPATH/bin:$PATH
fi


# added by travis gem
[ -f /Users/kalmi/.travis/travis.sh ] && source /Users/kalmi/.travis/travis.sh

[ -f ~/.fzf.bash ] && source ~/.fzf.bash

# ftags - search ctags
ftags() {
  local line
  [ -e tags ] &&
  line=$(
    awk 'BEGIN { FS="\t" } !/^!/ {print toupper($4)"\t"$1"\t"$2"\t"$3}' tags |
    cut -c1-80 | fzf --nth=1,2
  ) && $EDITOR $(cut -f3 <<< "$line")
}
